import { FoundMediaDirective } from './found-media.directive';

describe('FoundMediaDirective', () => {
  it('should create an instance', () => {
    const directive = new FoundMediaDirective();
    expect(directive).toBeTruthy();
  });
});
