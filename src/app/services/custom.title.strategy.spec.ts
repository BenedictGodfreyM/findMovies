import { CustomTitleStrategy } from './custom.title.strategy';

describe('CustomTitleStrategy', () => {
  it('should create an instance', () => {
    expect(new CustomTitleStrategy()).toBeTruthy();
  });
});
